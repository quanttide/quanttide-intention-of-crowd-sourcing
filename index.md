# 工作意图

> 冷启动策略：自供需求（量潮作为首个/唯一需求方）+ 单垂直切入 → 再横向开放成平台。

## 目标（本设计要解决的三件事）

1. **可判定的交付/验收标准** —— 把「有效」从形容词变成机器可判定的布尔公式，发布时写死并公开。
2. **明码标价 + 平台托管** —— 定价表公开、验收通过才打款，用托管机制解决「接单者凭什么信你能拿到钱」的信任问题。
3. **唯一归因** —— 解决「一笔成交归谁」的纠纷源头。

这三件事互为前提：**验收标准是钥匙**——先定义明白「什么叫有效」，托管结算与归因才有客观依据。

## 首期最小闭环（MVP）

```
发布任务（验收配方 + 定价表 + 托管预算池）
  → 接单（worker 领取/申请）
  → 提交证据（submission.evidence）
  → 自动验收（verification_runs：硬规则全跑 + 软规则量化质量分 + 人工抽样复核）
  → 托管结算（accepted → settle → release，从 budget_pool 划款）
  → 信誉（pass_rate / on_time_rate / dispute_rate）
```

**MVP 的全部工作量集中在「验收器 verifier」。** 验收器立住后，托管结算、唯一归因、信誉、平台化扩展都随之顺理成章。

## 定位取舍（关键决策）

| 决策 | 取舍 | 依据 |
|---|---|---|
| 通用 vs 单垂直 | 通用任务模型，但**首期只实现 sale 一种类型** | 避免通用抽象拖慢销售种子；接口最小可用即可 |
| 定价 | 标准化任务包 + 明码标价 | 可规模化的众包前提（反对「分佣面议」） |
| 结算 | 平台托管（escrow）+ 分阶段（线索费/成交佣金） | 解决信任与长周期现金流 |
| 归因 | 成交唯一归属 + 线索分档激励 | 简单可验证，减少纠纷；不做多触点加权 |
| 首期重点 | 交付与验收标准 | 定义清楚「有效线索/有效成交」，避免扯皮与拒付 |

## 边界（首期不做）

多任务类型、竞价/招标、多触点加权归因、评论审核、复杂信誉等级、消息通知、开放给第三方发任务。
